name: laravel-lab

services:
    db:
        image: mysql:8.4
        ports:
            - "${DB_PORT}:3306"
        volumes:
            - mysqldata:/var/lib/mysql
        environment:
            - MYSQL_ROOT_PASSWORD=${DB_PASSWORD}
            - MYSQL_DATABASE=${DB_DATABASE}

volumes:
    mysqldata:
